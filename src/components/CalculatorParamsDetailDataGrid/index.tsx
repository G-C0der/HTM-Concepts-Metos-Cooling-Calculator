import React from 'react';
import {CalculatorParams} from "../../types";

interface CalculatorParamsDetailDataGridProps {
  params: CalculatorParams;
}

const CalculatorParamsDetailDataGrid = ({ params }: CalculatorParamsDetailDataGridProps) => {
  return (
    <></>
  );
};

export {
  CalculatorParamsDetailDataGrid
};